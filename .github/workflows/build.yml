name: Build
on:
  push:
    branches:
      - main
      - develop

  pull_request:
    branches:
      - main
      - develop

jobs:
  build-android:
    name: Build Android
    runs-on: ubuntu-latest
    env:
      GRADLE_OPTS: "-Xmx16g -XX:MaxMetaspaceSize=1024m"
    permissions:
      pull-requests: write

    steps:
      - name: Setup Environment (PR)
        if: ${{ github.event_name == 'pull_request' }}
        shell: bash
        run: echo "SHA_SHORT=$(echo ${{ github.event.pull_request.base.sha }} | cut -c1-7)" >> $GITHUB_ENV
      - name: Setup Environment (Push)
        if: ${{ github.event_name == 'push' }}
        shell: bash
        run: echo "SHA_SHORT=$(echo ${{ github.sha }} | cut -c1-7)" >> $GITHUB_ENV

      - name: Check out Git repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Generate build files
        run: npx expo prebuild --platform android

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build Debug APK
        working-directory: android
        run:
          ./gradlew assembleDebug

      - name: Upload Debug APK
        id: upload-debug
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-${{ env.SHA_SHORT }}
          path: android/app/build/outputs/apk/debug/*.apk

      - name: Build Release APK
        working-directory: android
        run:
          ./gradlew assembleRelease

      - name: Upload Release APK
        id: upload-release
        uses: actions/upload-artifact@v4
        with:
          name: app-release-${{ env.SHA_SHORT }}
          path: android/app/build/outputs/apk/release/*.apk

      - name: Comment on PR
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          message: |
            [Download Debug APK](${{ steps.upload-debug.outputs.artifact-url }})
            
            [Download Release APK](${{ steps.upload-release.outputs.artifact-url }})
            
            > Latest commit: [${{ env.SHA_SHORT }}](https://github.com/${{ github.repository }}/commit/${{ github.event.pull_request.base.sha }})            
